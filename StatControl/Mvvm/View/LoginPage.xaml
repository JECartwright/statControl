<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StatControl.Mvvm.View.LoginPage">

    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HorizontalOptions="CenterAndExpand">
            <Entry Placeholder="Please Enter Your Steam ID..." Text="{Binding SteamProfileIdText}" HorizontalOptions="CenterAndExpand" />
            <Button Command="{Binding HomePageCommand}" Text="{Binding HomePageCommand.Text}"/>
            <Label Text="{Binding ErrorMsgText}" TextColor="Red" IsVisible="{Binding ErrorMsgTextVisible}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>